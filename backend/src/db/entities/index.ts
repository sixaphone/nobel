import { UserEntity } from './user.entity';
import { ClientEntity } from './client.entity';
import { ServiceEntity } from './service.entity';

export const Entities = [UserEntity, ClientEntity, ServiceEntity];

export default Entities;
